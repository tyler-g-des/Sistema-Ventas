@model IEnumerable<PrimerParcial.Models.Entidades.Ciudad>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Descripcion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Pais)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Descripcion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Pais.idPais)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.idCiudad">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.idCiudad">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.idCiudad">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
