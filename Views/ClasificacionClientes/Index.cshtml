@model IEnumerable<PrimerParcial.Models.Clasificaciones.ClasificacionCliente>

@{
    ViewData["Title"] = "Index";
}

<h2>Clasificacion Clientes</h2>

<p>
    <a class="btn btn-success" asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TipoDeCliente)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TipoDeCliente)
            </td>
            <td>
                <a class="btn btn-primary" asp-action="Edit" asp-route-id="@item.idClasificacionCliente">Edit</a> |
                <a class="btn btn-secondary" asp-action="Details" asp-route-id="@item.idClasificacionCliente">Details</a> |
                <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.idClasificacionCliente">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
