@model IEnumerable<PrimerParcial.Models.Clasificaciones.ClasificacionSuplidor>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TipoDeSuplidor)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TipoDeSuplidor)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.idClasificacionSuplidor">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.idClasificacionSuplidor">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.idClasificacionSuplidor">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
