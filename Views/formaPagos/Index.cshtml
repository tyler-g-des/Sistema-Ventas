@model IEnumerable<PrimerParcial.Models.Entidades.formaPago>

@{
    ViewData["Title"] = "Index";
}

<h2>Forma de Pago</h2>

<p>
    <a class="btn btn-success" asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.formaPagoDescripcion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.formaPagoDescripcion)
            </td>
            <td>
                <a class="btn btn-primary" asp-action="Edit" asp-route-id="@item.formaPagoID">Edit</a> |
                <a class="btn btn-secondary" asp-action="Details" asp-route-id="@item.formaPagoID">Details</a> |
                <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.formaPagoID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
